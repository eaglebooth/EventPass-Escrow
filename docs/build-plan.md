# EventPass Escrow Build Plan

1. Implement sender-bound, payable ticket escrow with immutable evidence commitments.
2. Add comparative GenLayer jury review and cross-field verdict validation.
3. Add bounded deadlines and role-balanced recovery for every nonterminal state.
4. Build the English pass board, ticket composer, action desk, protocol, and contract pages.
5. Wire real Studionet reads/writes through `genlayer-js` with runtime address override.
6. Run syntax, invariant, frontend build, and browser checks before any deployment.

